function Footer({ total }) {
  return (
    <footer className="footer">
      <p>Total Students: {total}</p>
      <p>Built with React state, props, and reusable components.</p>
    </footer>
  );
}

export default Footer;
